class AppStrings {
  AppStrings._();

  /// One of the 5 sanctioned places the real app name "Billing Project"
  /// is hardcoded (CLAUDE.md App Name Rule). Every other place in the
  /// app must reference this constant — never hardcode the string.
  static const appName = 'Billing Project';
  static const appTagline = 'Send bills. Grow your business.';
  static const continueBtn = 'Continue';
  static const sendBillBtn = 'Send on WhatsApp';
  // add more strings as we build screens
}
