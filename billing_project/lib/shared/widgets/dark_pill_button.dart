import 'package:flutter/material.dart';

class DarkPillButton extends StatelessWidget {
  const DarkPillButton({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
